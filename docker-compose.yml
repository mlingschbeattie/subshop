version: '3.8'
services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./db/freshsqueeze.db:/app/db/freshsqueeze.db
    environment:
      - NODE_ENV=development
    restart: unless-stopped
